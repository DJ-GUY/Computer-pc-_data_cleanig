# Computer-pc-_data_cleanig
ETL processes to transform raw data into a structured data warehouse.  I started by designing a star schema to structure a flat 10,000-row PC sales dataset, then built and validated the warehouse step by step.  This involved creating dimension and fact tables, cleaning and formatting the data, and ensuring consistency across key fields
